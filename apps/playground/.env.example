# Anthropic API key for Claude models
ANTHROPIC_API_KEY=sk-ant-...

# NextAuth secret — generate with: openssl rand -base64 32
AUTH_SECRET=

# PostgreSQL connection string (Vercel Postgres, Neon, Supabase, etc.)
POSTGRES_URL=

# Optional: Plaid sandbox credentials used as fallback when users
# don't provide their own. Lets visitors try API tools immediately.
PLAID_CLIENT_ID=
PLAID_SECRET=
